# Root deps for the GitHub Actions data pipeline (data/run.py and friends).
# The FastAPI app has its own peekorobo-api/requirements.txt.
requests>=2.31.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.10
tqdm>=4.66.0
tenacity>=8.2.0
pytz>=2024.1
